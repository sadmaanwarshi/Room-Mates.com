<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Dropdown</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&family=Pacifico&family=Playwrite+IS:wght@100..400&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900&display=swap");
      /* Navbar with gradient background */
      .navbar {
        background: linear-gradient(135deg, #6a11cb, #2575fc);
      }
      .navbar-brand,
      .nav-link,
      .dropdown-toggle {
        color: #fff !important;
      }
      .navbar img {
        width: auto;
        height: 65px;
        margin-top: -23px;
        margin-bottom: -23px;
        /* border-radius: 50%; */
        margin-right: 240px;
        padding-left: -15px;
        position: relative;
        /* right: -26px; */
      }
      .navbar-brand {
        font-size: 22px;
        font-family: "Pacifico", serif;
      }
      .navbar-toggler-icon {
        background-image: url("data:image/svg+xml;charset=UTF8,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3E%3Cpath stroke='black' stroke-width='2' stroke-linecap='round' stroke-miterlimit='10' d='M4 7h22M4 15h22M4 23h22'/%3E%3C/svg%3E");
      }
      .navbar-toggler {
        border: none; /* Remove the border if desired */
      }
      #navbarNavDropdown ul {
        font-family: "Lato", serif;
        font-weight: 700;
      }
      .navbar-nav .nav-link {
        color: #fff;
        transition: color 0.3s ease;
      }

      .navbar-nav .nav-link:hover {
        color: #6a11cb; /* Change to your preferred hover color */
      }

      .dropdown-menu .dropdown-item {
        color: #212529; /* Default color for dropdown items */
        transition: color 0.3s ease;
      }

      .dropdown-menu .dropdown-item:hover {
        color: #0061ff; /* Change to your preferred hover color */
        background-color: #e9ecef; /* Optional: Add a subtle background color on hover */
      }

      /* Dropdown menu styling */
      .dropdown-menu {
        border-radius: 10px;
        padding: 1rem;
        min-width: 250px;
        text-align: center;
        background: #f8f9fa;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
      }
      .dropdown-header {
        font-size: 1.2rem;
        font-weight: bold;
        color: #6a11cb;
      }
      .profile-email {
        font-size: 0.9rem;
        color: gray;
      }

      /* Custom button styling */
      .btn-custom {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 5px;
        width: 100%;
        transition: background 0.3s;
      }
      .btn-custom:hover {
        background: #2575fc;
        color: #fff;
      }

      /* Map styling */
      #map {
        height: 400px; /* Set height of the map */
        width: 100%; /* Make map responsive */
      }
    </style>

    <script src="https://maps.ola.com/sdk/v1.0/ola-map-sdk.js"></script>
    <!-- Ola Maps SDK -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg">
      <div class="container-fluid">
        <a class="navbar-brand" href="#"
          ><img src="/assets/images/logo.png" alt="Logo"
        /></a>
        <!-- <a class="navbar-brand" href="#" >Roomiess-Finder</a> -->
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNavDropdown"
          aria-controls="navbarNavDropdown"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <a
                class="nav-link active"
                aria-current="page"
                href="/pg/owner/dashboard?user=<%= user.id %>"
                >Home</a
              >
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Help</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Pricing</a>
            </li>
          </ul>

          <!-- User Icon Dropdown -->
          <div class="dropdown">
            <a
              href="#"
              class="nav-link dropdown-toggle"
              id="userDropdown"
              role="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
            >
              <i class="bi bi-person-circle" style="font-size: 1.5rem"></i>
            </a>
            <ul
              class="dropdown-menu dropdown-menu-end"
              aria-labelledby="userDropdown"
            >
              <!-- Centered Profile Info -->
              <li>
                <h6 class="dropdown-header">
                  <%= user.owner_name ? user.owner_name: "Guest" %>
                </h6>
              </li>
              <li>
                <span class="profile-email"
                  ><%= user.email ? user.email : "Not logged in" %></span
                >
              </li>
              <li><hr class="dropdown-divider" /></li>
              <li>
                <a
                  class="dropdown-item btn-custom btn-outline-primary btn-sm"
                  href="#"
                  ><i class="bi bi-person"></i> Profile</a
                >
              </li>
              <li>
                <button class="btn btn-outline-primary btn-custom btn-sm mb-2">
                  <i class="bi bi-gear"></i> Settings
                </button>
                <a
                  href="/logout"
                  class="btn btn-outline-danger btn-custom btn-sm"
                  ><i class="bi bi-box-arrow-right"></i> Logout</a
                >
              </li>
            </ul>
          </div>
        </div>
      </div>
    </nav>

    <script
      src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"
      integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"
      integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
